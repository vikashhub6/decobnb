<%- include('../partials/head') %>
  </head>
  <body class="bg-gray-100 min-h-screen">
    <%- include('../partials/nav') %>

    <div class="bg-white shadow-sm px-8 py-8 text-center">
      <h2 class="text-5xl font-extrabold text-red-500 mb-2 tracking-wide">🏠 Welcome to DecorBnB</h2>
      <p class="text-gray-500 text-lg mb-6">Find & book the most beautiful decorated spaces near you</p>
      <div class="w-24 h-1 bg-red-400 mx-auto mb-6 rounded-full"></div>
      <div class="flex items-center bg-gray-100 rounded-xl px-4 py-3 max-w-2xl mx-auto">
        <i class="fas fa-search text-gray-400 mr-3"></i>
        <input type="text" id="searchInput" placeholder="Search events, locations..." 
          class="bg-transparent w-full outline-none text-gray-700 text-base" onkeyup="filterEvents()"/>
      </div>
    </div>

    <main class="w-full px-8 py-10">
      <div id="eventList" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        <% registeredevents.forEach(event => { %>
          <div class="event-card bg-white rounded-xl shadow-md overflow-hidden hover:shadow-xl transition duration-300"
               data-name="<%= event.EventName %>"
               data-location="<%= event.location %>">
            <img src="<%= event.photo %>" alt="<%= event.EventName %>" class="w-full h-52 object-cover">
            <div class="p-4">
              <h3 class="text-xl font-semibold text-gray-800 mb-1"><%= event.EventName %></h3>
              <p class="text-gray-500 mb-2 text-sm"><i class="fas fa-map-marker-alt mr-1 text-red-400"></i><%= event.location %></p>
              <div class="flex justify-between items-center mb-3">
                <span class="text-lg font-bold text-red-500">Rs<%= event.price %> / night</span>
                <div class="flex items-center gap-1">
                  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5 text-yellow-400">
                    <path fill-rule="evenodd" d="M10.788 3.21c.448-1.077 1.976-1.077 2.424 0l2.082 5.007 5.404.433c1.164.093 1.636 1.545.749 2.305l-4.117 3.527 1.257 5.273c.271 1.136-.964 2.033-1.96 1.425L12 18.354 7.373 21.18c-.996.608-2.231-.29-1.96-1.425l1.257-5.273-4.117-3.527c-.887-.76-.415-2.212.749-2.305l5.404-.433 2.082-5.006z" clip-rule="evenodd" />
                  </svg>
                  <span class="text-gray-700 font-medium"><%= event.rating %></span>
                </div>
              </div>
              <div class="flex flex-col gap-2">
                <button class="w-full bg-green-400 hover:bg-green-500 text-white font-semibold py-2 px-4 rounded-lg transition duration-200">Book Now</button>
                <a href="/events/<%= event._id %>" class="w-full bg-blue-400 hover:bg-blue-500 text-white font-semibold py-2 px-4 rounded-lg transition duration-200 text-center block">View Details</a>
                <%- include('../partials/favourite', {event: event}) %>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    </main>

    <script>
      function filterEvents() {
        const query = document.getElementById('searchInput').value.toLowerCase();
        document.querySelectorAll('.event-card').forEach(card => {
          const name = card.dataset.name.toLowerCase();
          const location = card.dataset.location.toLowerCase();
          card.style.display = (name.includes(query) || location.includes(query)) ? 'block' : 'none';
        });
      }
    </script>

  </body>
</html>